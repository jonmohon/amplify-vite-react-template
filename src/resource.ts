export type Schema = {

    Lead: {
  
      id: string;

      email: string;
  
      createdAt: string;
  
      updatedAt: string;
  
      // Add other lead properties based on your GraphQL schema
  
    };
  
  };
  