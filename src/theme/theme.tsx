// src/theme.ts
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#0A89FF', // Blue background color
    },
    secondary: {
      main: '#ffffff', // White text color
    },
  },
});

export default theme;
